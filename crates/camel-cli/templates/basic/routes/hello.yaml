# Sample route for camel test — see routes/hello.test.yaml
routes:
  - id: "hello"
    from: "direct:start"
    steps:
      - set_header:
          key: "source"
          value: "hello"
      - to: "mock:result"

---

Resumen de lo pendiente/futuro que emergen de los planes:

| Ítem                                                                 | Origen                                 | Prioridad implícita                                                                                                            | Estado |
| -------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Graceful shutdown / drain                                            | Tower reconnection Non-Goals           | Alta — importante para producción                                                                                              | DONE   |
| Backpressure (poll_ready always-ready)                               | Tower reconnection Non-Goals           | Alta — necesario para sistemas de carga real                                                                                   | DONE   |
| Circuit breaker                                                      | Error handling Non-Goals               | Media                                                                                                                          | DONE   |
| Splitter EIP                                                         | Core design + Tower reconnection       | Media — primer EIP con sub-pipeline                                                                                            | DONE   |
| File component (consumer + producer)                                 | Core design Future Extensions          | Media — primer componente I/O real                                                                                             | DONE   |
| HTTP component (producer)                                            | Core design Future Extensions          | Media — integración con APIs externas                                                                                          | DONE   |
| WireTap EIP                                                          | Splitter design Future EIPs            | Media-baja — Exchange clone + fire-and-forget, casi trivial                                                                    | DONE   |
| Aggregator EIP                                                       | Splitter design Future EIPs            | Media —                                                                                                                        | DONE   |
| Multicast EIP                                                        | Splitter design Future EIPs            | Media — mismo patrón que Splitter (clone N, aggregate)                                                                         | DONE   |
| Stop EIP (.stop() dentro de filter)                                  | Apache Camel docs                      | Baja — Filter NO hace full stop (es correcto así según Camel);                                                                 | DONE   |
|                                                                      |                                        | para parar el pipeline hace falta un Stop EIP explícito dentro del bloque                                                      |        |
| Aggregator v2: completion by timeout                                 | Aggregator design Deferred             | Media-baja — requiere `tokio::spawn` interno por bucket + `CancellationToken`                                                  |        |
| Aggregator v2: correlation key por función                           | Aggregator design Deferred             | Baja — cambiar `header_name: String` a `CorrelationKeyFn`;                                                                     |        |
|                                                                      |                                        | exponer `correlate_by_header` y `correlate_by_fn` en el builder                                                                |        |
| Aggregator v2: estado compartido cross-route                         | Aggregator design Deferred             | Baja — requiere registry de agregadores nombrados en `CamelContext`                                                            |        |
| Aggregator v2: estado persistente                                    | Aggregator design Deferred             | Baja — buckets son in-memory, se pierden en restart                                                                            |        |
| Aggregator v2: `forceCompletionOnStop`/`discardOnAggregationFailure` | Aggregator design Deferred             | Baja — opciones Java Camel diferidas                                                                                           |        |
| WireTap v2: prepare/processor function                               | WireTap postmortem                     | Baja — transformar copia antes de enviar; Apache Camel tiene esto                                                              |        |
| WireTap v2: synchronous tap with timeout                             | WireTap design Non-Goals               | Baja — viola fire-and-forget, añade complejidad                                                                                |        |
| Routing Slip EIP                                                     | Core design                            | Media-baja                                                                                                                     |        |
| HTTP consumer (server/webhook endpoint)                              | File-HTTP plan implícito               | Media-baja — HttpConsumer implementado con Axum; soporta múltiples routes/port                                                 | DONE   |
| Idempotent file consumer (seen-file tracking)                        | File-HTTP postmortem \#6               | Media-baja — noop=true re-envía duplicados cada poll, necesita idempotent repo                                                 |        |
| Pipeline concurrency (Option E typestate)                            | Pipeline concurrency analysis          | Media-baja — struct-based consumers con concurrency compile-time                                                               | DONE   |
| HttpOperationFailed: añadir URL y método                             | File-HTTP postmortem \#7               | Baja — mejora de debugging en logs                                                                                             |        |
| Recipient List EIP                                                   | Splitter design Future EIPs            | Baja — Multicast dinámico                                                                                                      |        |
| Multicast v2: onPrepare processor                                    | Multicast design Future Enhancements   | Baja — transformar copia por endpoint antes de enviar; Apache Camel tiene esto                                                 |        |
| Multicast v2: streaming mode (out-of-order)                          | Multicast design Future Enhancements   | Baja — agrega resultados en orden de llegada, no de envío                                                                      |        |
| Multicast v2: shareUnitOfWork                                        | Multicast design Future Enhancements   | Baja — coordinar transacciones entre endpoints del multicast                                                                   |        |
| StepAccumulator trait (DRY SplitBuilder/RouteBuilder)                | Splitter postmortem Task 4             | Baja — refactoring puro, sin impacto funcional; ahora afecta 4 builders:                                                       | DONE   |
|                                                                      |                                        | RouteBuilder, SplitBuilder,                                                                                                    |        |
|                                                                      |                                        | y cualquier builder futuro que añada `.aggregate()`                                                                            |        |
| Parallel stop_on_exception con future cancellation                   | Splitter design Non-Goals + postmortem | Baja — fallback funcional existe (join_all, retorna primer error)                                                              |        |
| Nested splits                                                        | Splitter design Non-Goals              | Baja — YAGNI                                                                                                                   |        |
| Streaming split (async Stream en vez de Vec)                         | Splitter design Non-Goals              | Baja — para splits muy grandes que no caben en memoria                                                                         |        |
| Adaptive circuit breaker                                             | Resilience design Non-Goals            | Baja — sliding windows, latency thresholds                                                                                     |        |
| Per-exchange retry state persistence                                 | Resilience design Non-Goals            | Baja — estado vive solo en memoria                                                                                             |        |
| Concurrency limiter / tower::Buffer                                  | Resilience design Non-Goals            | Baja — YAGNI con single pipeline task/route                                                                                    |        |
| Full pull-based pipeline (reemplazar ch 256)                         | Resilience design Non-Goals            | Baja — backpressure implícito es suficiente                                                                                    |        |
| XML/YAML DSL para circuit breaker config                             | Resilience design Non-Goals            | Baja — diferido a camel-dsl                                                                                                    |        |
| Type converters                                                      | Core design Future Extensions          | Baja                                                                                                                           |        |
| camel-dsl (YAML/JSON routes)                                         | Core design + Error handling Non-Goals | Baja — crate ya existe vacío                                                                                                   | DONE   |
| Más components (kafka, etc.)                                         | Core design Future Extensions          | Baja — Redis implementado; Kafka y otros dependen de casos de uso                                                              |        |
| Management API (health, metrics)                                     | Core design Future Extensions          | Baja — camel-health ya existe vacío                                                                                            |        |
| Tracer (message flow tracing)                                        | Apache Camel Tracer                    | Media — tracing automático del flujo de mensajes                                                                               | DONE   |
| Tracer: OpenTelemetry integration                                    | Tracer design Future Enhancements      | Media-baja — `OpenTelemetryOutput` preparado en config pero sin implementar; requiere `camel-component-otel`                   |        |
| Tracer: `OutputFormat::Plain`                                        | Tracer postmortem                      | Baja — variante definida pero no implementada; actualmente solo JSON funciona                                                  |        |
| Tracer: performance benchmarks                                       | Tracer design doc                      | Baja — planeados en design doc pero no implementados; medir overhead de `compose_traced_pipeline` vs `compose_pipeline`        |        |
| Choice/When/Otherwise EIP (Content-Based Router)                     | Apache Camel docs / Core design        | Media — dedicated CBR EIP, replaces Filter composition workaround                                                              | DONE   |
| Log EIP (.log() DSL method)                                          | Apache Camel Log EIP                   | Alta — logging simple para mensajes humanos                                                                                    | DONE   |
| RouteController / autoStartup / ControlBus                           | AutoStart plan                         | Alta — lifecycle management de rutas                                                                                           | DONE   |
| SupervisingRouteController                                           | AutoStart postmortem + Future Work     | Media — reintentos automáticos cuando consumer crasha; `route_controller.rs:450` tiene TODO: status no se actualiza a `Failed` | DONE   |
| Supervision: métricas (restarts/attempts por ruta)                   | SupervisingRouteController postmortem  | Baja — el loop no reporta métricas al MetricsCollector; contar reinicios y fallos por ruta                                     |        |
| Supervision: callbacks (on_crash / on_restart hooks)                 | SupervisingRouteController postmortem  | Baja — API para que el usuario registre handlers cuando una ruta falla o se reinicia                                           |        |
| Supervision: status API (query attempts por ruta)                    | SupervisingRouteController postmortem  | Baja — no hay forma de consultar cuántos intentos lleva una ruta sin inspeccionar el loop interno                              |        |
| Supervision: backoff jitter                                          | SupervisingRouteController postmortem  | Baja — sin jitter, múltiples rutas que caen simultáneamente se reinician al mismo tiempo (thundering herd)                     |        |
| Supervision: threshold de éxito configurable                         | SupervisingRouteController postmortem  | Baja — actualmente usa `initial_delay` como threshold para resetear intentos; debería ser configurable                         |        |
| Supervision: shutdown explícito del loop                             | SupervisingRouteController postmortem  | Baja — el loop sale cuando el Arc se dropea; un CancellationToken explícito daría shutdown determinista post-`stop()`          |        |
| `suspend_route` / `resume_route` reales                              | AutoStart postmortem                   | Media-baja — suspend/resume real implementado (consumer-only pause, pipeline alive, stop full shutdown preservado)             | DONE   |
| ControlBus: acción `stats`                                           | AutoStart Future Work                  | Baja — devolver métricas de ruta (exchanges procesados, uptime, etc.)                                                          |        |
| Lifecycle timeout configurable para `suspend`/`stop`                 | Suspend/Resume postmortem (Future)     | Baja — hoy timeout de lifecycle está hardcodeado (30s), conviene hacerlo configurable por contexto/ruta                        |        |
| Métricas de lifecycle (`suspend`/`resume`)                           | Suspend/Resume postmortem (TODO)       | Baja — exponer contador de suspensiones/reanudaciones, duraciones y timeouts                                                   |        |
| Pause/Resume nativo en trait `Consumer`                              | Suspend/Resume postmortem (Deferred)   | Media-baja — evitar recreación de consumer en resume y preservar estado interno por componente                                 |        |
| Stress tests de ControlBus (`suspend/resume` concurrente)            | Suspend/Resume postmortem (Deferred)   | Baja — validar robustez bajo ciclos rápidos y acciones concurrentes                                                            |        |
| Tests de integración: aislar paralelismo (serial_test o similar)     | AutoStart postmortem                   | Media-baja — `camel-test` tests se cuelgan con `cargo test` por defecto; workaround actual: `--test-threads=1`                 | DONE   |
| Redis component (producer + consumer)                                | Core design Future Extensions          | Media — implementado con soporte strings, hashes, lists, sets, sorted sets, pubsub                                             | DONE   |
| camel-cli: package name reservation                                  | camel-cli plan                         | Baja — CLI mínima publicada en crates.io para reservar nombre; sin funcionalidad real aún                                      | DONE   |
| Languages subsystem (Simple + Rhai)                                  | Apache Camel Languages                 | Media — Language/Expression/Predicate traits, Simple Language, Rhai scripting                                                  | DONE   |
| Simple Language v2: `${body.fieldName}` JSON path                    | Languages postmortem                   | Media-baja — DONE: `Expr::BodyField(Vec<PathSegment>)` + dot-notation + array indexing; solo `Body::Json`                     | DONE   |
| Simple Language v2: mixed interpolation (`"text ${expr} more"`)       | Languages postmortem                   | Media — DONE: `Expr::Interpolated(Vec<InterpolatedPart>)` + plain text StringLit fallback; `log: "Got ${body}"` funciona       | DONE   |
| Rhai Language: propagar `set_header`/`set_property` al Exchange real | Languages postmortem                   | Media-baja — actualmente solo afectan el scope in-eval; propagación requiere pasar `&mut Exchange` a la función registrada     |        |
| Language DSL integration: `.set_body(simple("..."))` en RouteBuilder | Languages postmortem                   | Baja — integrar Language/Expression en el builder DSL para uso directo en rutas                                                |        |
| `camel-language-bean`: llamar métodos de structs desde expresiones   | Apache Camel Bean language             | Baja — `bean("MyService", "processOrder")` evalúa en contexto de Exchange                                                      |        |
| Hot-reload: ReloadCoordinator execution                              | ArcSwap plan + postmortem              | Media — DONE: `execute_reload_actions` in `reload.rs`; watcher in `reload_watcher.rs`; `camel-cli run` subcommand              | DONE   |
| Hot-reload: in-flight request tracking                              | ArcSwap plan notes                     | Baja — test coverage actual usa controller layer; demo con running consumer real requeriría component registration             |
| Hot-reload: debounce configurable                                    | hot-reload postmortem                  | Baja — hardcoded 300ms en `reload_watcher.rs:83`; debería exponerse en `Camel.toml` vía `camel-config` (campo `watch_debounce_ms`) |        |
| YAML DSL: más step types (filter, split, choice, etc.)               | ArcSwap plan note \#3                  | DONE — implementado en DSL step contract; todos los mandatory steps soportados                                                | DONE   |
| YAML DSL: error handler config                                       | Resilience design Non-Goals            | DONE — `DeclarativeErrorHandler` con `dead_letter_channel` + `retry` soportado                                                | DONE   |
| DSL Contract: canonical declarative IR                               | DSL step contract plan                  | Alta — single source of truth para semántica de rutas declarativas                                                            | DONE   |
| DSL Contract: compile-time enforcement                               | DSL step contract plan                  | Alta — `assert_contract_coverage()` garantiza sincronización de DSLs                                                          | DONE   |
| DSL Contract: language-backed expressions                            | DSL step contract plan                  | Alta — `DeclarativeFilter`, `DeclarativeChoice`, `DeclarativeScript` resueltos via registry                                   | DONE   |
| DSL Contract: camel-config delegation                                | DSL step contract plan                  | Media — `camel-config/yaml.rs` ahora re-exporta de `camel-dsl`                                                                 | DONE   |
| DSL Contract: README updates                                         | DSL step contract postmortem            | DONE — camel-dsl y camel-config READMEs actualizados con referencia completa                                                | DONE   |
| DSL Contract: DeclarativeSplit language-backed expression            | DSL step contract postmortem            | DONE — `SplitExpressionDef::Language` soporta `simple:` y `rhai:`                                                            | DONE   |
| DSL Contract: DeclarativeAggregate completion predicate              | DSL step contract postmortem            | DONE — campos añadidos; timeout/predicate aún sin implementar (error claro)                                                  | DONE   |
| DSL Contract: more language shortcuts in YAML                        | DSL step contract postmortem            | DONE — documentado en READMEs y ejemplo yaml-dsl                                                                             | DONE   |
| Route_id: compile-time enforcement (typestate pattern)               | ArcSwap postmortem                     | Baja — validación actual es runtime; typestate evitaría errores en examples                                                    |        |
| Hot-reload: Skip action con hash comparison                          | ArcSwap postmortem                     | Baja — actualmente siempre Swap si from_uri igual; Skip evitaría swaps innecesarios pero requiere comparar steps               |        |
| Streaming Body: native Axum response streaming                       | Gap 3 postmortem                       | Media — evitar materialización en HttpConsumer (\`into_bytes\`); requiere actualizar HttpReply para ser Stream-aware           |        |
| Streaming Body: intelligent clone (re-open file)                     | Gap 3 postmortem                       | Media-baja — permitir que Body::clone() reabra archivos si \`origin\` es conocido para evitar single-consumption limit         |        |
| Streaming Body: EIP error propagation                                | Gap 3 postmortem                       | Baja — refinar cómo Multicast/Splitter manejan errores de stream en vez de usar placeholders de texto                          |        |
| Streaming Body: Direct streaming to File                             | Gap 3 review                           | Media — usar tokio::io::copy para volcar stream a disco directamente en FileProducer sin pasar por Vec<u8>                     |        |

## Log EIP — Behavior Change
`log:` steps in YAML DSL now evaluate their message as Simple Language (consistent with Apache Camel).
Plain strings like `log: "Hello"` continue to work; strings with `${...}` are now interpolated.

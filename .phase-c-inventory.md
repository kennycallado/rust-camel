# Phase C Inventory (scratch — NOT committed)

## Baseline (Task 0, 2026-06-08)

- Allowlist entries: **62** ✓ (matches plan)
- Inline escapes: **0** ✓ (matches plan)
- `allow-inline.max`: 17 (plan said 0 — stale, Task 14 deletes)
- `allowlist-log-levels.txt.max`: 98 (plan said 62 — stale, Task 14 deletes)
- rc-d7a: in_progress, claimed by opencode
- Worktree: `.worktrees/adr-0012-second-wave` (from main `4ef655ef`)
- Branch: `adr-0012-second-wave`

## Drift (Task 1, 2026-06-08)

60/62 entries verified. 2 drifts in `crates/camel-dsl/src/yaml.rs`:

| Allowlist entry | Actual line | Content | Annotation status |
|---|---|---|---|
| `yaml.rs:70` | **L76** | `error!(error = %e, "yaml parse failed")` | already has `// log-policy: system-broken` |
| `yaml.rs:1211` | **L1278** | `error!(path = %path.display(), error = %e, "failed to load routes from file")` | TBD by Task 9 |

**Cluster affected:** Task 9 (Infra). Worker must update both allowlist entries to new line numbers when handling camel-dsl migration.

## Notes

- `.max` files stale — upper bounds not violated. Phase C Task 14 deletes both files when strict-mode flips.
- Allowlist drift is normal between Phase B squash merge and Phase C execution; worker just needs to re-grep current lines per file.

# Allowlist for `cargo xtask lint-log-redaction`.
# Format: <relative path>:<line>  (one entry per line; `#` comments ignored)
# Inline alternative: append `// allow-log-redaction` to the offending line.
# Use sparingly — each entry is an accepted unredacted url/uri/config log.

# ── camel-component-grpc (rc-redactstrict task 2.2) ─────────────────────
# consumer.rs:488 — macro logs `host = %host` (clone of self.host): bare gRPC
#   consumer hostname from config, no URL and no credential substrate.
crates/components/camel-component-grpc/src/consumer.rs:488
# consumer.rs:618 — `host = %self.host`: bare gRPC consumer hostname from
#   config, no URL and no credential substrate.
crates/components/camel-component-grpc/src/consumer.rs:618
# consumer.rs:641 — `host = %self.host`: bare gRPC consumer hostname from
#   config, no URL and no credential substrate.
crates/components/camel-component-grpc/src/consumer.rs:641
# tls_reload.rs:47 — macro logs `host = %self.host`: bare TLS listener
#   hostname from config, no URL and no credential substrate.
crates/components/camel-component-grpc/src/tls_reload.rs:47
# tls_reload.rs:56 — macro logs `host = %self.host`: bare TLS listener
#   hostname from config, no URL and no credential substrate.
crates/components/camel-component-grpc/src/tls_reload.rs:56

# ── camel-http (rc-redactstrict task 2.2) ───────────────────────────────
# lib.rs:1193 — `host = host`: bare HTTP server hostname parameter
#   (&str), no URL and no credential substrate.
crates/components/camel-http/src/lib.rs:1193
# lib.rs:2166 — `host = %self.config.host`: bare configured HTTP bind
#   hostname, no URL and no credential substrate.
crates/components/camel-http/src/lib.rs:2166
# tls_reload.rs:60 — macro logs `host = %self.host`: bare HTTP TLS server
#   hostname from config, no URL and no credential substrate.
crates/components/camel-http/src/tls_reload.rs:60
# tls_reload.rs:69 — macro logs `host = %self.host`: bare HTTP TLS server
#   hostname from config, no URL and no credential substrate.
crates/components/camel-http/src/tls_reload.rs:69

# ── camel-redis (rc-redactstrict task 2.2) ──────────────────────────────
# config.rs:400 — `host = %self.host`: bare Redis hostname from config
#   (hostname only; credentials ride the separate password field), no URL
#   and no credential substrate.
crates/components/camel-redis/src/config.rs:400
# config.rs:840 — `host = %self.host.as_deref().unwrap_or("")`: bare Redis
#   hostname from config (hostname only; credentials ride the separate
#   password field), no URL and no credential substrate.
crates/components/camel-redis/src/config.rs:840

# ── camel-ws (rc-redactstrict task 2.2) ─────────────────────────────────
# Line refs renumbered by rc-onm5b (ServerRegistry liveness eviction insertions).
# lib.rs:583 — `host = %bound_addr.ip()`: bare listener IP from SocketAddr,
#   provably credential-free; wrapping a non-URL IP in redact_url is
#   semantically wrong (disposition b).
crates/components/camel-ws/src/lib.rs:583
# lib.rs:612 — `host = %bound_addr.ip()`: bare listener IP from SocketAddr,
#   provably credential-free; wrapping a non-URL IP in redact_url is
#   semantically wrong (disposition b).
crates/components/camel-ws/src/lib.rs:612
# lib.rs:624 — `host = %bound_addr.ip()`: bare listener IP from SocketAddr,
#   provably credential-free; wrapping a non-URL IP in redact_url is
#   semantically wrong (disposition b).
crates/components/camel-ws/src/lib.rs:624
# lib.rs:704 — `host = %addr.ip()`: bare listener IP from SocketAddr,
#   provably credential-free; wrapping a non-URL IP in redact_url is
#   semantically wrong (disposition b).
crates/components/camel-ws/src/lib.rs:704
# lib.rs:1728 — `host = self.cfg.inner.host`: bare configured WebSocket
#   hostname, no URL and no credential substrate.
crates/components/camel-ws/src/lib.rs:1728
# lib.rs:1760 — `host = self.cfg.inner.host`: bare configured WebSocket
#   hostname, no URL and no credential substrate.
crates/components/camel-ws/src/lib.rs:1760
# lib.rs:1794 — `host = self.cfg.inner.host`: bare configured WebSocket
#   hostname, no URL and no credential substrate.
crates/components/camel-ws/src/lib.rs:1794
# lib.rs:1802 — `host = self.cfg.inner.host`: bare configured WebSocket
#   hostname, no URL and no credential substrate.
crates/components/camel-ws/src/lib.rs:1802
# lib.rs:1961 — `host = canonical_host`: canonical host derived from config
#   (hostname/IP only, see WsUriConfig::canonical_host), no credential
#   substrate.
crates/components/camel-ws/src/lib.rs:1961
# lib.rs:1982 — `host = canonical_host`: canonical host derived from config
#   (hostname/IP only, see WsUriConfig::canonical_host), no credential
#   substrate.
crates/components/camel-ws/src/lib.rs:1982

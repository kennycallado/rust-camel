# Allowlist for `cargo xtask lint-log-redaction`.
# Format: <relative path>:<line>  (one entry per line; `#` comments ignored)
# Inline alternative: append `// allow-log-redaction` to the offending line.
# Use sparingly — each entry is an accepted unredacted url/uri/config log.

# Empty since rc-c1qwn follow-up (mission 173 / rc-3y929): all seven
# entries (camel-component-grpc tls_reload.rs 47/56, camel-http lib.rs
# 1193 + tls_reload.rs 60/69, camel-redis config.rs 400/840) were
# redeemed at source via camel_api::redact::redact_host — clean hosts
# pass through unchanged, userinfo-shaped input is masked (ADR-0076).
# Line numbers above are the lint's keys: the macro's first-argument
# line, not the `host =` field line. Empty-allowlist probe: 0 violations.

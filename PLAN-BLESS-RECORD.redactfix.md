# Plan-bless self-grill record — redactfix (fleet mission 94, conductor-light STAGE 2)

- Plan under blessing: `openspec/changes/redactfix/tasks.md` (single phase, tasks 1.1 + 1.2)
- Plan hash verified: `sha256:21a907d35cf71f1ae89fbde4bf14e7866daaa13617f14ff863175541cf7305c4` (recomputed via `scripts/bless-hash.sh` — exact match)
- Skill: self-grill-proposals (non-interactive grill-with-docs adaptation)
- Verdict: **BLESS** (one non-blocking archive-time note, Q5 below)

## Questions generated

1. [glossary] Do the plan's terms (authority window, sentinel, fail-closed, guard test) conflict with CONTEXT-MAP.md glossary entries?
2. [sharpen] Is any instruction fuzzy enough that a w_fast/w_balanced worker must guess (fixtures, assertions, signature, placement)?
3. [scenario] What constructed input breaks the planned contract or the fail-first/guard classification? (grill Q1+Q2)
4. [cross-ref] Does the code today match what the plan claims changes? Scenario↔test bijection? Task 1.2 acceptance verifiable, zone intact? (grill Q3+Q4)
5. [technique-specific] Is spec scenario 5's mailto looseness acceptable for archive-time merge, or must the spec amend before plan-bless? (grill Q5)

## Answers (with citations)

1. [glossary] No conflicts. "Fail-closed" is established vocabulary (CONTEXT-MAP.md:128, ADR-0033; also :81,:82). The governing law is "Credential redaction boundary — Authority: ADR-0051" (CONTEXT-MAP.md:179): credential-bearing URLs must not expose values at diagnostic boundaries — the plan enforces exactly this. "Authority window" and sentinel `[redacted]` are locally defined (spec.md:9-11, design.md:14-20) and the sentinel literal already exists in current code (lib.rs:2906 `?[redacted]`). Outcome: confirm.
2. [sharpen] No guessing required. Fixtures are verbatim strings; expected outputs are literals (`"[redacted]"`, `http://host:99999/path?[redacted]`); new-symbol signature exact (`fn truncate_utf8_safe(s: &mut String, max: usize)`, private, placed after `redact_url_for_diagnostics` at lib.rs:2892); test placements anchored (~3669 tests mod, ~8748 after `armed_fence_rejects_unknown_host_redacted` — actual 8746, within "~"); commands and pre/post pass/fail matrix given. Truncate-site count accurate: exactly 3 `s.truncate(MAX_URL_LOG_LEN)` sites exist (lib.rs:2908-2909, 2914-2915, 2920-2921) — 2 Ok-arm + 1 Err-arm, matching task steps 2-3. Outcome: confirm.
3. [scenario] Empirical probe against url 2.5.8 (Cargo.lock) confirmed every fixture classification: 4 suppression fixtures → Err (empty host / invalid port ×2 / relative-without-base); bd-repro `http://user:pa%ss@host/path` → **Ok** (`pass=Some("pa%ss")`, round-trip identical) → GUARD correct (current Ok arm masks: lib.rs:2897-2900 `set_username("***")`); `mailto:user@example.com` → **Ok**, empty userinfo, byte-identical round-trip → GUARD/Ok-arm note correct; `http://host:99999/x@y` → Err. Simulated planned Err-arm algorithm reproduces every asserted output: 4× sentinel; short query → `http://host:99999/path?[redacted]`; long (319B pre-query) → len 256, zero `longsecret` bytes; straddle (401B, `a`+`é`×200) → 255, `is_char_boundary(255)`=true, within [253,256]; current impl panics on straddle (probe reproduced `assertion failed: self.is_char_boundary(new_len)`). Fail-first split accurate: suppression + short-query fixtures leak under current Err arm (raw truncate, lib.rs:2920-2922); long-query sub-case happens to satisfy its two assertions pre-fix but the test fails via the short fixture — consistent with the task's test-level (not assertion-level) fail-first claim. Fence assertions hold: `uri_host_allowed` returns `Ok(false)` on parse failure (lib.rs:2705-2707); error literal contains `allowedUriHosts fence` (lib.rs:2543-2546); planned output for the 1.2 fixture is bare `[redacted]` → no `fencesecret`, no `u:`. Outcome: confirm.
4. [cross-ref] Current code matches the plan's premise: Err arm echoes raw truncated (lib.rs:2919-2923); Ok arm masking/query/cap as spec scenario 7 pins (lib.rs:2894-2917); four existing `redact_url_*` tests at 3669/3692/3698/3715 named exactly, `redact_url_truncates_unparseable` fixture is `"x".repeat(1000)` asserting len 256 — matches spec scenario 3's example. Scenario↔test bijection: S1→suppresses, S2→bd_repro, S3→straddle+existing truncates, S4→short_and_long, S5→at_sign, S6→task 1.2 fence, S7→existing four; reverse mapping also total (no orphan tests). Task 1.2 acceptance verifiable: commit 1.1 first, then diff — `git diff --stat` file-level plus hunk inspection confirms tests-only; zone intact — both tasks touch only `crates/components/camel-http/src/lib.rs`; BUSY zones (camel-dsl, camel-cli) zero-diff; no new deps. Outcome: confirm.
5. [scenario/technique-specific — Q5 ruling] Spec scenario 5's GIVEN loosely lists `mailto:user@example.com` under strings that "fail url::Url::parse" while mailto parses Ok (probe-verified). Judgment: **acceptable for archive-time; do not amend before plan-bless.** Grounds: (a) the THEN-contract ("not suppressed; renders under query-redaction and cap rules only") holds under both arms — probe-verified byte-identical round-trip; (b) tasks.md states the true semantics explicitly and forbids the one concrete trap (is_err precondition on mailto, tasks.md:27), so implementation cannot inherit the confusion; (c) the requirement prose itself is correctly arm-scoped (spec.md:5-24) — only the scenario's example list misattributes; (d) any future test derived from the scenario passes against ground truth, so the looseness is cosmetic, not contractual; (e) re-opening a round-3 blessed spec (e_gpt) for a wording normalization costs a full re-bless round for zero behavioral delta. Action (non-blocking): during archive/spec-sync, tighten S5's GIVEN to "`http://host:99999/x@y` (parse failure), and a scheme-only string like `mailto:user@example.com` (parses as cannot-be-a-base, exercising the parse-success arm) — the THEN-contract holds under both arms." Outcome: confirm with archive-time note.

## Outcome

**confirm** — plan blessed for implementation. Worker: w_balanced for 1.1 (justified: multi-assertion test design, single file), tests-only worker for 1.2. No plan edits required.

Self-grill mode: self-grill-proposals skill. Evidence: source read at lib.rs:2499-2546, 2701-2710, 2892-2925, 3669-3722, 8746-8790; url 2.5.8 empirical probe (8 fixtures + algorithm simulation + current-impl panic repro); bless-hash.sh recompute (exact match).

# Tasks: grpclik

All paths relative to repo root. Work happens in the
`camel-component-grpc` crate only. Spec delta:
`openspec/changes/grpclik/specs/grpc-consumer-cancellation/spec.md`.
bd: rc-qq8zz.

## camel-component-grpc — bidi response stream closes on consumer shutdown/abort

### Task 1.1: integration regression tests — open bidi stream must close on shutdown and abort (RED phase)

**Files:**
- `crates/components/camel-component-grpc/tests/integration.rs` (modified)

**Steps:**
1. Read the existing helpers first: `open_bidi_call` (integration.rs:1931) and `wait_for_total` (integration.rs:1913), and the permitcancel tests `grpc_consumer_shutdown_during_saturated_permit_wait_exits_cleanly` / `grpc_consumer_abort_mid_permit_wait_allows_same_path_restart` for the consumer-harness pattern (staged listener, `GrpcConsumer::new`, `ConsumerContext::new` with `with_in_flight_counter`, `start_with_listener`). Mirror that pattern exactly; do not invent a new harness.
2. Add `grpc_consumer_shutdown_closes_open_bidi_response_stream`: SETUP — bind a staged listener on `127.0.0.1:0`, note the port (never a bind-read-drop probe); `GrpcConsumer::new("127.0.0.1", port, "/streaming.StreamService/BidiEcho", "tests/streaming.proto", "streaming.StreamService", "BidiEcho", GrpcMode::Bidi, test_rt(), GrpcServerConfig::default(), 1)` (arg shapes mirror the existing tests); `counter = Arc<new AtomicU64(0)>`; `(route_tx, _route_rx) = mpsc::channel(16)` (keep `route_rx` alive, never read it — with zero client chunks nothing is ever sent on the route channel; A's acceptance claim is held call-scoped by the parked processor future); `cancel_token = CancellationToken::new()`; `ctx = ConsumerContext::new(route_tx, cancel_token.clone(), "grpc-bidi-close-route".into()).with_in_flight_counter(Arc::clone(&counter))`; spawn `consumer_task` running `consumer.start_with_listener(ctx, listener).await.expect("consumer start")`; ONE annotated readiness sleep (`100 ms`, comment `// allow-test-sleep: readiness for bidi-close consumer test (rc-qq8zz)`); connect `StreamServiceClient` and open client A via `open_bidi_call` — send NO message, keep the returned `echo_tx` alive for the whole test (the client request stream stays open). Acceptance proof: `assert!(wait_for_total(&counter, 1, 5))` — A's call-scoped acceptance claim (envelope dequeued, processor spawned, parked on `body_rx`). ACT — `cancel_token.cancel()`. ASSERT — (a) `timeout(Duration::from_secs(2), consumer_task)` completes with `Ok(())`; (b) `timeout(Duration::from_secs(2), item_rx.recv())` yields `Some(Err(status))` with `status.code() == tonic::Code::Unavailable` (the client request side is STILL open — this is the pre-fix leak), then `timeout(1s, item_rx.recv())` yields `None`; (c) `assert!(wait_for_total(&counter, 0, 2))`.
3. Add `grpc_consumer_abort_closes_open_bidi_response_stream`: SETUP identical to step 2 (own staged listener/port, route name `"grpc-bidi-abort-route"`, client A open and idle, `wait_for_total(&counter, 1, 5)`). ACT — `consumer_task.abort()` then `let _ = timeout(Duration::from_secs(2), consumer_task).await` (JoinError ignored, join bounded like the mirrored permitcancel test). ASSERT — (a) `timeout(Duration::from_secs(2), item_rx.recv())` yields a terminal item — `Some(Err(_))` with ANY code or `None` (do not pin the code on the abort path; the client request side is STILL open); (b) `assert!(wait_for_total(&counter, 0, 2))`.
4. Bounded-wait discipline: every `recv()`/`.await` on externally-driven progress inside the two tests is wrapped in `tokio::time::timeout`. No unbounded waits, no loops without deadlines. The ONLY sleep sites are the two annotated readiness sleeps (one per test).
5. Run both new tests and CONFIRM the RED failure mode is exactly the bounded timeout at the first `item_rx.recv()` assertion (2 s), not a compile error and not an earlier assertion. If they fail any other way, STOP and report a `test-design-gap:` message describing the gap instead of adjusting the test to pass.

**Tests:** (executable spec)
- `grpc_consumer_shutdown_closes_open_bidi_response_stream`: ARRANGE accepted bidi call, client request side open, counter at 1 → ACT `cancel_token.cancel()` → ASSERT consumer joins ≤2 s; first response item ≤2 s is `Err(Unavailable)` with client side still open; then `None`; counter drains to 0. Command: `cargo test -p camel-component-grpc --test integration grpc_consumer_shutdown_closes_open_bidi_response_stream`. Expected: FAILS pre-fix (2 s timeout at the item assertion), PASSES after Task 1.2.
- `grpc_consumer_abort_closes_open_bidi_response_stream`: ARRANGE accepted bidi call, client request side open, counter at 1 → ACT `consumer_task.abort()` → ASSERT first response item ≤2 s is terminal (any outcome) with client side still open; counter drains to 0. Command: `cargo test -p camel-component-grpc --test integration grpc_consumer_abort_closes_open_bidi_response_stream`. Expected: FAILS pre-fix (2 s timeout), PASSES after Task 1.2.

**Acceptance:**
- Both new tests exist, compile, and FAIL with the expected bounded-timeout failure mode (RED confirmed by observed output, quoted in the task report).
- All OTHER integration tests still green: `cargo test -p camel-component-grpc --test integration -- --skip grpc_consumer_shutdown_closes_open_bidi_response_stream --skip grpc_consumer_abort_closes_open_bidi_response_stream` exits 0.
- `cargo fmt --check` clean; `cargo clippy -p camel-component-grpc --all-targets -- -D warnings` exits 0; `cargo xtask lint-test-sleep` and `cargo xtask lint-unbounded-wait` exit 0 (run from the worktree root).

- [x] 1.1

### Task 1.2: cancel wiring — dispatch-entry child token, guard cancellation, gated bidi forward task (GREEN phase)

**Files:**
- `crates/components/camel-component-grpc/src/server.rs` (modified)
- `crates/components/camel-component-grpc/src/consumer.rs` (modified)
- `crates/components/camel-component-grpc/CONTEXT.md` (modified)

**Steps:**
1. `server.rs`: widen `pub(crate) type GrpcDispatchEntry` (server.rs:33) from the 3-tuple to `(mpsc::Sender<GrpcRequestEnvelope>, GrpcMode, Option<Arc<GrpcKernelAuth>>, tokio_util::sync::CancellationToken)`.
2. `consumer.rs`: hoist `const SHUTDOWN_STATUS: &str = "consumer shutting down"` out of `reply_unavailable` (consumer.rs:276) to a `pub(crate)` module-level const so server.rs can reuse it; `reply_unavailable` references the hoisted const (single source for the shutdown status string).
3. `consumer.rs` `start_inner` (consumer.rs:654): after the `env_tx_for_guard` clone (consumer.rs:683), mint `let handler_cancel = ctx.cancel_token().child_token();` — NEVER `CancellationToken::new()` in production code (lint-cancel-tokens ratchets fresh roots under `crates/components/*/src`). Pass `handler_cancel.clone()` into `insert_dispatch_entry` and `DispatchRegistrationGuard::arm`.
4. `consumer.rs` `insert_dispatch_entry` (consumer.rs:387): add a `cancel: tokio_util::sync::CancellationToken` parameter, insert the 4-tuple `(env_tx, mode, kernel, cancel)`. Duplicate-path and stale-replacement semantics unchanged.
5. `consumer.rs` `DispatchRegistrationGuard` (consumer.rs:321): add field `cancel: tokio_util::sync::CancellationToken`; `arm` takes it; `cleanup()` calls `self.cancel.cancel()` after removal + disarm; `Drop::drop` calls `self.cancel.cancel()` as its FIRST statement — unconditional, before the `if !self.armed` early-return and before the runtime-conditional `try_write`/spawn match (blessed design: `cancel()` is synchronous and runtime-free; the no-runtime teardown branch must still cancel, and double-cancel is idempotent). Entry-removal logic below it is untouched.
6. `server.rs` `handle_grpc_request` (server.rs:547): the table lookup clones all four fields; destructure `Some((sender, mode, kernel, cancel))`; `GrpcMode::Bidi` arm constructs `BidiHandler { sender, kernel, cancel }`; the unary/server-streaming/client-streaming handlers are unchanged (they ignore the token).
7. `server.rs` `BidiHandler` (server.rs:758): add field `cancel: tokio_util::sync::CancellationToken`; in `call`, move a clone into the spawned forward task and restructure its body to:
   `loop { tokio::select! { biased; _ = cancel.cancelled() => { let _ = reply_tx_forward.try_send(GrpcStreamItem::Error(tonic::Status::unavailable(crate::consumer::SHUTDOWN_STATUS))); break; } result = stream.next() => { let Some(result) = result else { break }; match result { Ok(bytes) => { if body_tx.send(bytes).await.is_err() { break; } } Err(status) => { tracing::warn!(error = %status, "bidi streaming decode error"); let _ = reply_tx_forward.send(GrpcStreamItem::Error(status)).await; break; } } } } }` — semantics of the client-stream arm are byte-identical to today; only the cancel arm is new. `try_send` (never `send().await`) on the cancel arm: shutdown must not block on a full/gone channel.
8. Update every existing test construction site for the widened shapes (grep for `GrpcMode::` tuple inserts and `BidiHandler {` in `#[cfg(test)]` blocks — server.rs ~985, 1024, 1038, 1063, 1300 and `BidiHandler` literals at 1341, 1404, 1569; consumer.rs `insert_dispatch_entry`/`arm` call sites ~1665, 1692, 1744, 1761): add `CancellationToken::new()` (test scope — allowed) or pass the fresh token through. No test-logic changes beyond the mechanical widening.
9. Add ONE shared test harness in server.rs `#[cfg(test)] mod tests` used by all three new bidi handler tests — no existing unit test constructs a `Request<Streaming<Vec<u8>>>` (the existing bidi tests only build the handler struct or push envelopes), so name it here: a test-local helper `open_bidi_request() -> (tonic::Request<tonic::Streaming<Vec<u8>>>, tokio::sync::mpsc::Sender<Vec<u8>>)` built on `tonic::Streaming::new_request` (public in tonic 0.14.x) with a passthrough `Decoder` (impl of `tonic::codec::Decoder` with `Item = Vec<u8>` that never errors) over an `http_body_util::StreamBody` fed by an mpsc the test NEVER closes — the request stream stays open exactly like an idle bidi client. `http-body-util` is already a dependency of the crate. If `Streaming::new_request` is not reachable with this tonic version, STOP and report a `test-design-gap:` message instead of improvising a different construction.
10. New unit tests in server.rs `#[cfg(test)] mod tests` (all three use the step-9 harness; envelope sender-accounting matters: the envelope holds the ORIGINAL `reply_tx`, so a test that wants a terminal `None` from the response stream MUST first recv the envelope from `env_rx` (bounded `timeout(500ms, ..)`) and drop it — tokio mpsc yields `None` only when the buffer is empty AND every sender is gone):
   - `bidi_handler_cancelled_token_closes_stream_without_client_end`
   - `bidi_handler_uncancelled_token_forwards_and_cancel_closes`
   - `bidi_handler_cancel_with_full_reply_channel_never_blocks_or_injects_error`
11. New unit tests in consumer.rs `#[cfg(test)] mod tests` next to the existing dispatch-guard tests (~1656):
    - `dispatch_guard_cancels_token_on_cleanup`
    - `dispatch_guard_cancels_token_on_drop`
12. `CONTEXT.md`: append one sentence to the consumer paragraph (the long paragraph at line 12, after the rc-orr73 sentence): bidi response streams now close on consumer shutdown and abort — the dispatch entry carries a child token of the consumer's `ConsumerContext` cancellation token, the registration guard cancels it on every teardown path, and the bidi forward task observes it and terminates the call with a best-effort UNAVAILABLE (bd rc-qq8zz). Match the file's existing prose register; no new section.

**Tests:** (executable spec)
- `grpc_consumer_shutdown_closes_open_bidi_response_stream` (Task 1.1): FAILS before steps 1-7, PASSES after. Command: `cargo test -p camel-component-grpc --test integration grpc_consumer_shutdown_closes_open_bidi_response_stream`.
- `grpc_consumer_abort_closes_open_bidi_response_stream` (Task 1.1): FAILS before steps 1-7, PASSES after. Command: `cargo test -p camel-component-grpc --test integration grpc_consumer_abort_closes_open_bidi_response_stream`.
- `bidi_handler_cancelled_token_closes_stream_without_client_end`: ARRANGE `BidiHandler { sender: env_tx (alive mpsc), kernel: None, cancel: token }` with the step-9 harness request stream (stays open, no client end); ACT `handler.call(request)` → take the returned `ResponseStream`, recv the envelope from `env_rx` (bounded `timeout(500ms, ..)`) and DROP it (its `reply_tx` must not pin the channel open), then `token.cancel()`; ASSERT `stream.next()` inside `timeout(500ms, ..)` yields `Some(Err(status))` with `status.code() == tonic::Code::Unavailable`, and the following `stream.next()` inside `timeout(500ms, ..)` yields `None`. Command: `cargo test -p camel-component-grpc --lib bidi_handler_cancelled_token_closes_stream_without_client_end`. Expected: does not compile against pre-change code (no `cancel` field); passes after steps 1-7.
- `bidi_handler_uncancelled_token_forwards_and_cancel_closes`: ARRANGE same handler with a fresh (live) token and the step-9 harness; ACT `handler.call(request)` → recv the envelope from `env_rx` (bounded), send two `Vec<u8>` chunks through the harness request channel, ASSERT both chunks arrive on the envelope's `body_rx` (two bounded `timeout(500ms, ..)` `recv()` calls — the forward task's actual forwarding direction; nothing writes response items in a unit test, so assert HERE, not on the response stream); then `token.cancel()` (do NOT end the request stream); ASSERT the response stream's `stream.next()` inside `timeout(500ms, ..)` yields `Some(Err(status))` with `status.code() == tonic::Code::Unavailable`, then (after dropping the envelope) `None` inside `timeout(500ms, ..)`. Command: `cargo test -p camel-component-grpc --lib bidi_handler_uncancelled_token_forwards_and_cancel_closes`. Expected: does not compile against pre-change code; passes after; also proves the uncancelled path is behavior-identical.
- `bidi_handler_cancel_with_full_reply_channel_never_blocks_or_injects_error`: ARRANGE `BidiHandler` with a live token and a channel-backed request stream that stays open; ACT `handler.call(request)` → take the returned `ResponseStream` AND the `GrpcRequestEnvelope::Bidi` from `env_rx` (bounded `timeout(500ms, ..)` recv); fill the envelope's `reply_tx` to capacity — 64 successful `try_send(GrpcStreamItem::Message(vec![1u8]))` calls, assert all accepted; then `token.cancel()` WITHOUT draining, and drop the envelope's `reply_tx` (the test holds one sender; only the forward task's clone must remain until cancel fires it). ASSERT within an overall `timeout(2s, ..)` window: the response stream yields exactly the 64 buffered `Ok` items and then `None` — an `Err`/UNAVAILABLE item NEVER appears (a `send().await` cancel arm would park until draining frees capacity and then inject the error as the 65th item; `try_send` fails silently on the full channel and the sender drop closes the stream). Command: `cargo test -p camel-component-grpc --lib bidi_handler_cancel_with_full_reply_channel_never_blocks_or_injects_error`. Expected: does not compile against pre-change code (no `cancel` field); passes after steps 1-7.
- `dispatch_guard_cancels_token_on_cleanup`: ARRANGE `GrpcDispatchTable` with an owned entry, `token = CancellationToken::new()`, `guard = DispatchRegistrationGuard::arm(table, path, tx, token.clone())`; ACT `guard.cleanup().await`; ASSERT `token.is_cancelled()` and the entry is removed (poll `table.read().await` under `timeout(1s, ..)`). Command: `cargo test -p camel-component-grpc --lib dispatch_guard_cancels_token_on_cleanup`. Expected: does not compile against pre-change code (`arm` takes no token); passes after.
- `dispatch_guard_cancels_token_on_drop`: ARRANGE armed guard with `token`, no cleanup; ACT `drop(guard)`; ASSERT `token.is_cancelled()` synchronously (no await needed — cancel is the first statement of `drop`) and entry removal observed under `timeout(1s, ..)`. Command: `cargo test -p camel-component-grpc --lib dispatch_guard_cancels_token_on_drop`. Expected: does not compile against pre-change code; passes after.
- Existing suites stay green unmodified (permitcancel regression pin): `cargo test -p camel-component-grpc --test integration grpc_consumer_shutdown_during_saturated_permit_wait_exits_cleanly` AND `cargo test -p camel-component-grpc --test integration grpc_consumer_abort_mid_permit_wait_allows_same_path_restart` AND `cargo test -p camel-component-grpc --test integration grpc_consumer_saturated_stop_then_restart_reregisters_same_path`.

**Acceptance:**
- `cargo test -p camel-component-grpc --lib` green (all new + existing unit tests).
- `cargo test -p camel-component-grpc --test integration` FULLY green (Task 1.1's two RED tests now pass; permitcancel's three untouched).
- No new production `CancellationToken::new()` under `crates/components/camel-component-grpc/src`: `cargo xtask lint-cancel-tokens` exits 0 with the production site count unchanged.
- `cargo fmt --check` clean; `cargo clippy -p camel-component-grpc --all-targets -- -D warnings` exits 0 with NO dead_code allowance.
- `cargo xtask lint-test-sleep` and `cargo xtask lint-unbounded-wait` exit 0 (ratchets unchanged).
- CONTEXT.md carries the bd rc-qq8zz sentence.

- [x] 1.2

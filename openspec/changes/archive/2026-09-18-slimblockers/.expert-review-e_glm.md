VERDICT: APPROVE-WITH-FINDINGS

# e_glm stage-4 expert review — mission 115 slimblockers

Reviewer: e_glm (stage-4 expert gate, owner ruling 2026-09-17)
Scope: complete diff 37ec0cb6..b87bc5ec (10 commits), blessed artifacts,
evidence, gate ledger. Independent verification performed in the worktree:
manifest forward-chain reads, per-commit camel-cli attribution, spec-delta
vs main-spec comparison, `openspec validate slimblockers --strict`, test
assertion coverage. No cargo builds run (per order); ledger results
spot-checked against the code, not re-executed.

## Mission-critical verifies

### 1. Golden no-regen pin mechanism — SOUND

The mechanism chain, verified in the manifests: camel-cli
`default = ["full"]` → `full` includes `http-static` →
`http-static = ["camel-bundles/http-static"]` (camel-cli/Cargo.toml:130,173)
→ camel-bundles `http-static` now carries the eight `dep:` entries while
`default` is unchanged. Default camel-cli builds therefore still activate
all eight bridges through feature-forwarding, and forwarding-seeded
activations render no `cargo tree -e features` lines — the only rendering
shape that keeps the fixture byte-identical. The empirical record is
consistent: golden green ×3 without regen at v0.49.0, the fixture
(tests/fixtures/default-deptree.txt, which contains the bridge-crate lines)
is untouched in the diff, and Cargo.lock has zero drift. The rejected
alternatives (workspace-table carry, named/implicit features in default)
each fail exactly one of {rendering cleanliness, slim droppability}; the
chosen shape is the unique intersection. Correctly exploited.

### 2. Transitional debt — COMPLETE AND HONEST

Inventory cross-checked across manifest comments, CONTEXT.md files, spec
deltas, and bd evidence, each with the named removal owner (the camel-cli
bridge-forward mission; bd rc-9720m stays open with the full forward-diet
description as the tracking anchor):

- http-static carrier for the eight bridges (camel-bundles Cargo.toml
  comment + CONTEXT.md + ADDED requirement).
- `cfg(feature = "default")` engine gates in camel-template, no named
  re-enable feature (spec scenario "no named re-enable surface this
  change").
- slim-http one-release alias, drop at 0.50 (manifest + CONTEXT + MODIFIED
  requirement).
- redis slim survivor via camel-config → camel-redis-repo (spec scenario +
  CONTEXT; out-of-zone, named).
- SLIM_FORBIDDEN_PREFIXES bridge re-add (bd-rc-9720m deferral section).
- In-workspace camel-template default-features flip (bd-rc-wcs3v deferral
  section).

I found no deferred surface without an owner. See findings for one
tracking nit.

### 3. Size delta interpretation — CORRECT READING, NOT AN UNMET GOAL

Independently confirmed: camel-cli's own `[dependencies]` unconditionally
pulls all eight bridge crates and camel-template (crates/camel-cli/Cargo.toml
lines 60–82). Those edges keep every crate in the slim closure regardless of
camel-bundles, so −60,224 B of 55.7 MB (dead-code elimination of the gated
registration glue) is the maximum this zone-bounded change can produce. The
mission order asked to "measure binary size before/after (report numbers)" —
reported, with an honest structural explanation and a cargo-tree inversion
proof. The binary-size win is deferred with a named owner, exactly like the
debt in item 2. This is the correct reading, not a missed acceptance.

### 4. Zone discipline — RESPECTED

Diff touches only camel-bundles (4 files), camel-template (4 files),
camel-cli (3 files), and openspec/changes/slimblockers/**. No camel-config,
camel-dsl, security-surface, or benchmark files anywhere in the diff.
camel-cli per-commit attribution: Cargo.toml and tests/feature_profiles.rs
change only in the rename commit fd97159f; CONTEXT.md changes in fd97159f
plus one stale-paragraph rewrite in the holistic-fixes commit b87bc5ec
(review-driven doc truthfulness — see finding 1 for the residual wording
drift in design.md). The post-rebase rename-only grant is respected in
substance; no behavioral camel-cli change exists.

### 5. Spec deltas — MERGE-CLEAN AND TRUTHFUL

`openspec validate slimblockers --strict` passes. The MODIFIED ×2
requirement names ("Slim profile excludes the controllable bridge set",
"Bridge features are individually selectable") exist verbatim in the main
cli-feature-profiles spec and are full requirement replacements with
scenarios; the ADDED ×3 (cli) + ×2 (template) names collide with no main
requirement. Delta content matches the code I read: the eight-scheme boot
assertion (jms, xslt, xj, cxf, sql, redis, opensearch + pre-existing ws)
covers every gated registration site; the alias-identity test
(`slim_alias_resolves_identically`) matches the "alias resolves for one
release" scenario; the engine-free surface (config, error, path_util, uri)
matches the lib.rs gates and the engine_free test.

### 6. Stop conditions — NONE

Nothing here should stop the master from landing. Code quality is
production-grade: consistent transitional-gate comments at every cfg site,
both-polarity tests (bundles-present vs slim boot; engine vs engine-free),
`required-features = ["default"]` on the engine-coupled integration tests,
and the path_util doc-link de-linked from the gated closure module so
no-default builds stay rustdoc-clean.

## Findings (Minor — none blocking)

1. **Minor — design.md scope wording is stale vs the final diff.**
   design.md §Affected crates states the camel-cli CONTEXT.md change is
   "the two slim-http mentions ... plus a one-line alias note. Nothing
   else in camel-cli changes this mission." The actual diff additionally
   rewrites the minijinja/bridges linkage paragraphs (commit b87bc5ec,
   holistic-review-driven and truthful — the old text would now be false).
   Content is correct; the design sentence understates it. Cosmetic
   doc-vs-diff drift; can be folded into the archive pass.

2. **Minor — the alias drop-at-0.50 has no tracked owner.** The
   `slim-http = ["slim-benchmarks"]` removal is documented in manifest
   comments, CONTEXT.md, and the MODIFIED requirement, but nothing named
   (bd or mission) owns executing the drop. Recommend the camel-cli
   bridge-forward mission (or a small chore bd) explicitly pick it up so
   it does not survive 0.50 by inertia.

## Gate ledger spot-checks

Cross-checked against the code, consistent throughout: feature_profiles
structure (golden test reads the fixture path asserted in the delta);
kafka feature set untouched in camel-bundles; no fixture or lockfile files
in the diff; openspec tasks 9/9 ticked with no unticked residue; parked
inbox file correctly absent until this verdict is recorded.

## Summary

The change does exactly what the blessed design says, in the only shape
cargo semantics and the lint rules permit, with byte-identical default
closure proven without fixture regeneration and every deferral named and
owned. Both findings are documentation-hygiene level. APPROVE landing
after the conductor records this verdict and the park report.
